descargar todos los archivos y guardarlos en una misma carpeta
abrir el terminal en WSL
y poner "python3 -m venv venv"
luego poner "source venv/bin/activate"
luego "pip install -r requirements.txt"
y por ultimo levantar la api "fastapi dev"
luego ingresar a:http://localhost:8000/docs para tener acceso de los cruds de la api
